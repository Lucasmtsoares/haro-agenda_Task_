<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Haroagenda"
        tools:targetApi="31">
        <!--
            android:name=".loginActivitys.LoginActivity"
            android:theme="@style/Theme.autenticar"
        -->
        <activity
            android:name=".navegacao.NavegacaoActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <!--activity
            android:name=".navegacao.NavegacaoActivity"
            android:exported="false"
            android:label="@string/title_activity_navegacao" /-->
        <activity
            android:name=".NotaCards"
            android:exported="true" />
        <activity
            android:name=".loginActivitys.RegistroActivity"
            android:exported="false"
            android:theme="@style/Theme.autenticar"
            />
        <!--
             -->
        <activity
            android:name=".NotaForm"
            android:exported="false" />
        <activity
            android:name=".NotaEdit"
            android:exported="false" />
    </application>

</manifest>